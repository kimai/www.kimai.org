---
title: "Twig function config() leaks server-wide secrets (LDAP bind password, SAML SP private key) via invoice/export templates"
date: "2026-04-27 21:31:22 +0000"
ghsa_id: "GHSA-vrqv-52x7-rm4v"
cve_id: 
severity: "low"
affected_version: "<= 2.55.0"
patched_version: "2.56.0"
author: "fg0x0"
published: "kevinpapst"
state: "published"
---

Kimai's Twig sandbox (`StrictPolicy`, used for admin-uploaded invoice and export templates) allow-lists the `config()` Twig function with no key filtering. 

Since `config(name)` delegates to `App\Configuration\SystemConfiguration::find($name)`, which has access to the entire configuration, any `ROLE_SYSTEM_ADMIN` who can upload a Twig template can therefore render server-wide secrets (e.g. LDAP bind password, SAML SP private key) into the invoice or export output, which is then delivered to whoever generates an invoice or export from that template (including lower-privileged users such as teamleads with invoice permissions). 

## Info

- The invoice and export Twig (PDF/HTML) renderers both enable the sandbox against `StrictPolicy` and pass the shared Twig environment - the one with the `config` function registered - into sandboxed rendering
- An admin who uploads a malicious invoice or export template therefore gets an unrestricted read primitive against `kimai.config`
- In OnPremise installations, the attacker template can be uploaded through the admin UI (ROLE_SUPER_ADMIN, permission `upload_invoice_template`) and later rendered by whoever generates an invoice or export for that template
- The rendered output is returned as the invoice PDF or HTML, the secrets land in a document that can be downloaded
- **Kimai Cloud is not affected** because Twig templates have to pass a manual review process

The following Kimai OnPremise deployments are affected:
1. You have SAML or LDAP configured in `kimai.yaml`
2. You have at least one malicious `System-Admin` 

## Solution

The `config()` function now always guards against accessing `saml.` and `ldap.` keys.
In sandboxed environments (invoice / export) the `config()` function now uses a short whitelist to prevent access to unknown variables.

Users should update to `2.56.0` or newer.


---


## Security Advisories

The `collections/_security/` collection has a dedicated workflow and writing style.

- Scope: these files are Kimai security advisories published through Jekyll
- Language: English only
- Format: Markdown with Jekyll frontmatter
- Source reference: each advisory maps to a GitHub advisory at `https://github.com/kimai/kimai/security/advisories/<ghsa_id>`
- Frontmatter is usually pre-generated; do not rewrite it unless the task is explicitly about correcting advisory metadata
- Use `collections/_documentation/developer/bughunter.md` as the local policy reference for how Kimai handles security reports and published advisories

When creating or updating a security advisory:

- Act as a security reviewer and improve the text for clarity and accuracy
- Write for self-hosted IT administrators, administrators who support Kimai users, and open-source developers
- Keep the text short, precise, and easy to assess
- Focus on the impact and affected behavior inside Kimai
- Do not include code samples, proof-of-concept payloads, or exploitation instructions
- Include enough technical detail for readers to understand scope, impact, and urgency
- Do not guess about the fix; if the change made in Kimai is unclear, verify it first or ask

Use the same body structure for every advisory:

- Frontmatter first
- A one- or two-sentence introduction that gives a quick overview
- A `## Info` section, preferably with a bullet list explaining the issue in more detail
- A `## Solution` section that explains what changed in Kimai and ends with the sentence ``Users should update to `<version>` or newer.``
